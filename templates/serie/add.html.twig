{% extends "base.html.twig" %}
{% block title %}Ajouter une série{% endblock %}
{% form_theme form 'serie/_collection.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Ajouter une série dans le catalogue</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Ajouter une série</button>

        {{ form_end(form) }}
    </div>
{% endblock %}

