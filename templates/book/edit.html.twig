{% extends 'base.html.twig' %}
{% block title %}Edition d'un livre
{% endblock %}
{% form_theme form 'book/_collection.html.twig' %}
{% block body %}
    <div class="container">
		<h1>Editer : {{book.title}}</h1>

		{{ form_start(form) }}

		{{ form_widget(form) }}

		<button type="submit" class="btn btn-primary">Modifier le livre</button>

		{{ form_end(form) }}
	</div>
{% endblock %}