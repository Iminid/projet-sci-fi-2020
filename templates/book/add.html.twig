{% extends "base.html.twig" %}
{% block title %}Ajouter un livre{% endblock %}
{% form_theme form 'book/_collection.html.twig' %}
{% block body %}
    <div class="container">
    <button  class="btn btn-text-info"><a href="{{ path('films_add') }}">Ajouter un film</a></button>
    <button  class="btn btn-text-info"><a href="{{ path('series_add') }}">Ajouter une séries</a></button>
        <h1>Ajouter un livre dans le catalogue</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Ajouter un livre</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
