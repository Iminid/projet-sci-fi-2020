{% extends "base.html.twig" %}
{% block title %} Ajouter un film
{% endblock %}
{% form_theme form 'film/_collection.html.twig' %}
{% block body %}
	<div class="container">
	<button  class="btn btn-text-info"><a href="{{ path('series_add') }}">Ajouter une séries</a></button>
	<button class="btn btn-text-info"><a href="{{ path('livres_add') }}">Ajouter un livre</a></button> 
		<h1>Ajouter un film dans le catalogue</h1> 

		{{ form_start(form) }}

		{{ form_widget(form) }}

		<button type="submit" class="btn btn-primary addbutton">Ajouter un film</button>

		{{ form_end(form) }}
	</div>
{% endblock %}


